spring:
  application:
    name: jobapp
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:jobappdb
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    show-sql: true